.DS_Store
.build/
.swiftpm/
DerivedData/
build/
*.xcodeproj/
!project.yml
*.xcworkspace/
xcuserdata/
*.xcuserstate

# Vendor working directory (Scripts/fetch-vendor.sh clones here, not for commit)
Vendor/.work/

# Vendor sources (fetched by script — duplicated in Sources/C*/, kept transient)
Vendor/libdeflate/
Vendor/zstd/

# Benchmark artifacts
Tests/Benchmarks/data/
Tests/Benchmarks/results/

# Sample / scratch archives — never commit produced compressed files
*.zip
*.knit
!Tests/**/*.zip
!Tests/**/*.knit

# OS metadata
.DS_Store

# Local env
.env
.env.local
